import React from 'react';

const imageBanner = () => {
    return (
        <div className="imageBanner">
            <button>Shop Now </button>
        </div>
    );
};

export default imageBanner;
